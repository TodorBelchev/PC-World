import { WarrantyExpirationDatePipe } from './warranty-expiration-date.pipe';

describe('WarrantyExpirationDatePipe', () => {
  it('create an instance', () => {
    const pipe = new WarrantyExpirationDatePipe();
    expect(pipe).toBeTruthy();
  });
});
